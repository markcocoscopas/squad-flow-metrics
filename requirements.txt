streamlit>=1.35.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.22.0
pyyaml>=6.0.1
jinja2>=3.1.4
pytest>=8.2.0
pytest-cov>=5.0.0

# PDF export (optional — HTML export works without this)
# WeasyPrint requires system libraries that are complex to install on Windows.
# See: https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
# Uncomment the line below once system libraries are in place:
# weasyprint>=62.0
