"""
Squad Flow Metrics — core analytics package.

All public symbols are pure functions or frozen dataclasses; no Streamlit
or UI imports live here.  The same engine can be wrapped in a CLI, a
scheduled job, or a different UI without modification.
"""
